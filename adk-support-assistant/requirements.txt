# Runtime dependencies for the ADK support assistant.
# Pinned to lower bounds; use `pip install -e .[dev]` for the editable install.
google-adk>=1.0.0
litellm>=1.40.0
pydantic>=2.7
fastapi>=0.110
uvicorn>=0.29
python-dotenv>=1.0
httpx>=0.27

# Dev / test
pytest>=8.0
pytest-asyncio>=0.23
ruff>=0.5
